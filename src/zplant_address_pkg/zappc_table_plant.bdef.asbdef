projection implementation in class ZAPPBP_C_TABLE_PLANT unique;
strict ( 2 );
use draft;
define behavior for ZAPPC_TABLE_PLANT alias ZappcTablePlant
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}
projection implementation in class ZBP_C_INTEGRATION_TAB unique;
strict ( 2 );
use draft;
define behavior for ZC_INTEGRATION_TAB alias ZcIntegrationTab
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}